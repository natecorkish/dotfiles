# Git aliases
alias gep='git commit --allow-empty -m "Empty push"'
alias gl='lazygit'
alias ncc='git add .; git commit -m "No comment commit"; git push'

# General aliases
alias szsh='source $HOME/.zshrc'
ls='ls -a  --color=tty'
grep='grep  --color=auto --exclude-dir={.bzr,CVS,.git,.hg,.svn}'

